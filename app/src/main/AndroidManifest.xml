<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alset.lecturer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/alset"
        android:label="@string/app_name"
        android:roundIcon="@drawable/alset"
        android:supportsRtl="true"
        android:theme="@style/Theme.ALSETLecturer">
        <activity android:name=".ViewClassesActivity"></activity>
        <activity
            android:name=".ViewAttendanceActivity"
            android:label="@string/title_view_attendance" />
        <activity
            android:name=".NewClassActivity"
            android:label="@string/title_activity_new_class" />
        <activity android:name=".UpdatePasswordActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.ALSETLecturer.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>